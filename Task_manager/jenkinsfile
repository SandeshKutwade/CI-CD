pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                dir('Task_manager/pom.xml')
                sh 'mvn clean package'
            }
        }
        
        stage('Test') {
            steps {
                echo 'Run your tests here'
                // Add test steps
            }
        }
       
    }
}